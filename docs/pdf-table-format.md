# question

```javascript
{
  "page": 1,    // 页码
  "path": "pdf_pages/1200694563.PDF_001.PDF",  // 文件路径
  "height": 841,        // PDF高度
  "width": 595          // PDF宽度
}
```

# answer

```javascript
[
  {
    "id":           // 题目ID
    "topleft": [200, 300],  // 表格左上角相对于页面左上角的坐标
    "width": 1000,  // 表格宽
    "height": 2000, // 表格高
    "grid": {
      "rows": [100, 200, 300, 400, 500, 600, 700, 800, 900, 1000, 1100, 1200, 1300, 1400, 1500, 1600, 1700, 1800, 1900],  // 表格各行相对于表格顶线的距离，有顺序
      "columns": [100, 200, 300, 400, 500, 600, 700, 800, 900]                                                            // 表格各列相对于表格左线的距离，有顺序
    },
    "merged": [     // 合并单元格记录。每个元素表示一个合并单元格。每个元素中的元素表示组成合并单元格的原始单元格的行列号（zero-indexed）
      [[0,0], [0,1]],
      [[0,8], [0,9], [1,8], [1,9]],
      [[3,0], [4,0], [5,0], [7,0], [8,0]]
    ]
  },
  ...
]
```
